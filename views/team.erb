<!DOCTYPE html>
<html>
 <head>
  <meta charset="UTF-8">
  <title>Basketball Team</title>
 </head>
 <body>
   <% @team.each do |key, value| %>
     <% key.to_sym %>
     <% if key == :name %>
       <h1><%= "#{@abbv[key]}: #{value}" %></h1>
     <% else %>
       <h2><%= "#{@abbv[key]}: #{value}" %></h2>
     <% end %>
   <% end  %>
 </body>
</html>
